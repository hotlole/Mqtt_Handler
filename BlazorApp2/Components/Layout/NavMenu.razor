<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css" rel="stylesheet" />
<style>
    
   
    /* Переключатель меню */
    .navbar-toggler {
        display: none; /* Скрываем стандартный переключатель */
    }

   

    /* Навигационные ссылки */
    .nav-item {
        margin: 0.5rem 0;
    }


        /* Эффект при наведении */
        .nav-link:hover {
            background-color: rgba(255, 255, 255, 0.1); /* Легкий белый фон при наведении */
            transform: translateX(5px); /* Сдвиг вправо при наведении */
        }



        

        .nav-link:active::after {
            transform: translate(-50%, -50%) scale(1);
        }

   
    

    /* Анимация пульсации */
    @@keyframes pulse {
        0% {
            transform: scale(1);
        }
        50% {
            transform: scale(1.05);
        }
        100% {
            transform: scale(1);
        }
    }

    /* Анимация для иконок при наведении */
    .nav-link:hover .bi {
        transform: scale(1.2); /* Увеличение иконок при наведении */
    }
</style>

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Xtronik project</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Mqtt-monitor">
                <span class="bi bi-list-nested" aria-hidden="true"></span> Mqtt-Monitor
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="MqttSender">
                <span class="bi bi-list-nested" aria-hidden="true"></span> Mqtt-Sender
            </NavLink>
        </div>
    </nav>
</div>